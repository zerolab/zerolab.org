baseURL = "https://zerolab.org/"
languageCode = "en"
title = "Stuff and nonsense"

theme = "zero"

[frontmatter]
  date  = [":filename", ":default"]

[permalinks]
  til = "/til/:slug/"

[outputs]
  home = ["HTML", "RSS"]

[outputFormats.RSS]
  baseName = "feed"
